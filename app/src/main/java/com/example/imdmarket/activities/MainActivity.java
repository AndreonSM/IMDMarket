package com.example.imdmarket.activities;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import com.example.imdmarket.R;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        // Add your main content initialization here

        // In the onCreate method, you would typically set up the UI components, initialize data, and perform any other tasks necessary for the main screen.
    }
}
